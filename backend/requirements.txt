fastapi==0.135.0
starlette>=0.49.1
uvicorn[standard]==0.34.3
sqlalchemy[asyncio]==2.0.41
asyncpg==0.31.0
alembic==1.15.2
yahooquery>=2.3.7,<3.0
pandas==2.2.3
# Pinned for the same reason pandas is, plus one of its own: the indicator
# contract fixtures are exact float comparisons, so a transitive numpy bump
# that shifts a last-ulp result fails the contract-freshness CI step and reads
# as a mystery contract break rather than a dependency change.
numpy==2.5.2
exchange-calendars==4.13.2
apscheduler==3.11.0
pydantic-settings==2.9.1
httpx>=0.27,<1.0
