name: Cleanup GHCR packages

on:
  schedule:
    # Every Sunday at 03:00 UTC
    - cron: "0 3 * * 0"
  workflow_dispatch:

permissions:
  packages: write

jobs:
  cleanup-old-sha-tags:
    name: Delete old SHA-tagged container versions
    runs-on: ubuntu-latest
    steps:
      - name: Delete old SHA versions (keep latest and dev)
        uses: actions/delete-package-versions@v5
        with:
          package-name: fibenchi
          package-type: container
          owner: jvanmelckebeke
          min-versions-to-keep: 5
          ignore-versions: "^(latest|dev)$"
